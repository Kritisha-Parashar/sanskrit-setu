# Copy to .env and fill in values.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydatabase
DB_USER=postgres
DB_PASSWORD=

PORT=5000

JWT_SECRET=
JWT_REFRESH_SECRET=

# Resend — https://resend.com
RESEND_API_KEY=
# onboarding@resend.dev only delivers to your Resend signup email. Verify a domain for real users.
RESEND_FROM_EMAIL=Sanskrit-Setu <onboarding@resend.dev>

# Shared secret for POST /api/cron/dispatch-email-notifications (header x-cron-secret)
CRON_SECRET=

GEMINI_API_KEY=
